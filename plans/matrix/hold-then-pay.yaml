# A row of the layer matrix: hold the booking, then pay it at the held total. It sets no route, cabin, date, journey,
# or party, so every layer reaches it. Book Flight checks the offer and the order against the search, and the Hold
# Then Pay slot checks the payment and the total. Cleanup cancels the order.
kind: recipe
metadata:
  prompt: Hold a booking, then pay it, for whatever the layers ask
  graphVersion: "1.0.0"
selection:
  workflow: Book Flight
  description: Held, then paid, for the layers' search
  choices:
    payment: Hold Then Pay
