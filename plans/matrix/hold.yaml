# A row of the layer matrix: hold the booking unpaid. It sets no route, cabin, date, journey, or party, so every
# layer reaches it. Book Flight checks the offer and the order against the search, and the Hold slot checks the
# deadlines. Cleanup cancels the order.
kind: recipe
metadata:
  prompt: Hold a booking for whatever the layers ask
  graphVersion: "1.0.0"
selection:
  workflow: Book Flight
  description: Held unpaid, for the layers' search
  choices:
    payment: Hold
